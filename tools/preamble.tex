\usepackage[sfdefault,lining]{FiraSans}
\usepackage{firamath-otf}
\usepackage{fontspec}
\usepackage{newunicodechar}
\usepackage{xcolor}
\usepackage{array}

\NewDocumentCommand{\x}{}{x_1}
\NewDocumentCommand{\y}{}{x_2}
\NewDocumentCommand{\z}{}{x_3}


\setmathfont{Latin Modern Math}%[range={\bot, \top}]

\usetikzlibrary{graphdrawing, quotes}
\usetikzlibrary{positioning}
\usetikzlibrary{graphs}
\usetikzlibrary{calc}
\usegdlibrary{trees}
\usegdlibrary{layered}
\usetikzlibrary{matrix}
\usetikzlibrary{decorations.pathreplacing}
\usetikzlibrary{shapes}

\definecolor{LilleGreen}{HTML}{22A666} % Couleur verte univ
\definecolor{LilleLightBlue}{HTML}{7ABAFF} % bleu univ
\definecolor{LilleDarkBlue}{HTML}{5862ED} % bleu 2 univ
\definecolor{LilleGray}{HTML}{63656A} % Gris
\definecolor{LillePink}{HTML}{FFB4D2}
\definecolor{LilleRed}{HTML}{FC535C}
\definecolor{background}{HTML}{EDEDED}

\tikzset{decision/.style={
    circle,
    draw=black,
    fill=orange!30,
    minimum width=0.75cm,
    execute at begin node={$},
    execute at end node={$}
  }}

\tikzset{empty/.style={
    circle,
    minimum width=0.75cm
  }}

\tikzset{times/.style={
    circle,
    draw=black,
    fill=green!25,
    execute at begin node = {$\times$},
    minimum width=0.75cm
  }}

\tikzset{union/.style={
    circle,
    draw=black,
    fill=orange!10,
    execute at begin node = {$\cup$},
    minimum width=0.75cm
  }}
\tikzset{uplus/.style={
    circle,
    draw=black,
    fill=orange!20,
    execute at begin node = {$\uplus$},
    minimum width=0.75cm
  }}


\tikzset{wedge/.style={
    circle,
    draw=black,
    fill=green!25,
    execute at begin node = {$\wedge$},
    minimum width=0.75cm
  }}
\tikzset{vee/.style={
    circle,
    draw=black,
    fill=blue!25,
    execute at begin node = {$\vee$},
    minimum width=0.75cm
  }}

\tikzset{gate/.style={
    circle,
    draw=black,
    execute at begin node = {$},
    execute at end node={$},
    minimum width=0.75cm
  }}


\tikzset{circuit/.style={
    trapezium,
    draw=black,
    execute at begin node = {$},
    execute at end node={$},
  }}


\tikzset{input/.style={
  rectangle,
  draw=black,
  fill=gray!25,
  execute at begin node={$},
  execute at end node={$},
  minimum width=0.5cm,
  minimum height=0.5cm
}}

\tikzset{top/.style={
    rectangle,
    draw=black,
    fill=LilleGreen!25,
    execute at begin node={$\top$},
    minimum width=0.5cm,
    minimum height=0.5cm
  }}

\tikzset{bot/.style={
    rectangle,
    draw=black,
    fill=LilleRed!25,
    execute at begin node={$\bot$},
    minimum width=0.5cm,
    minimum height=0.5cm
  }}

\tikzset{cbot/.style={
    rectangle,
    draw=black,
    fill=LilleRed!25,
    minimum width=0.5cm,
    minimum height=0.5cm,
    execute at begin node={$},
    execute at end node={$}
  }}


\tikzset{table/.style={
    matrix of nodes,
    row sep=-\pgflinewidth,
    column sep=-\pgflinewidth,
    nodes={
      align=center,
      font=\small
    },
    nodes in empty cells,
    row 1/.style={
      nodes={
        font=\small\bfseries
      }
    }
  }
}

\tikzset{nrel/.style={
    rectangle,
    font=\small\color{LilleDarkBlue}
  }}

%%% Local Variables:
%%% mode: latex
%%% TeX-master: t
%%% End:
